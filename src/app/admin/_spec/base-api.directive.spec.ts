import { BaseApiDirective } from '../base-api.directive';

describe('BaseApiDirective', () => {
  it('should create an instance', () => {
    const directive = new BaseApiDirective();
    expect(directive).toBeTruthy();
  });
});
